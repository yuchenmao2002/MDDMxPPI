# PPIL training environment (Python >=3.10; Python 3.11 is recommended).
#
# H100/H200 note: install a PyTorch build that matches the CUDA driver/modules
# on the target cluster, following the cluster policy or the official PyTorch
# installer.  This file intentionally does not select a ``+cuXXX`` wheel,
# ``nvidia-*`` runtime packages, ``cudatoolkit``, or a CUDA package index.
# Installing the matching PyTorch build first is safe: the constraint below
# will accept it instead of replacing it when ``pip install -r`` is run.

# Model and single-GPU training runtime.
torch>=2.2,<3.0
safetensors>=0.4.2,<1.0

# Backed .h5ad input and dense/sparse minibatch conversion.
numpy>=1.26,<3.0
scipy>=1.11,<2.0
pandas>=2.1,<3.0
h5py>=3.10,<4.0
anndata>=0.10.8,<1.0

# Contract and numerical tests under tests/.
pytest>=8.0,<10.0
